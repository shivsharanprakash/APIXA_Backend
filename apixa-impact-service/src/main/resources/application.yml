server:
  port: 8088

spring:
  application:
    name: apixa-impact-service
  datasource:
    url: jdbc:sqlite:./data/impact.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    open-in-view: false
  threads:
    virtual:
      enabled: true

apixa:
  service-name: apixa-impact-service
