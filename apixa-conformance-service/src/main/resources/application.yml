server:
  port: 8085

spring:
  application:
    name: apixa-conformance-service
  datasource:
    url: jdbc:sqlite:./data/conformance.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    open-in-view: false
  threads:
    virtual:
      enabled: true

apixa:
  service-name: apixa-conformance-service
