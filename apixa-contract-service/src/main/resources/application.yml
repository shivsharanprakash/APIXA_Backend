server:
  port: 8082

spring:
  application:
    name: apixa-contract-service
  datasource:
    url: jdbc:sqlite:./data/contract.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    open-in-view: false
  threads:
    virtual:
      enabled: true

apixa:
  service-name: apixa-contract-service
