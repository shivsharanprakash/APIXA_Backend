server:
  port: 8083

spring:
  application:
    name: apixa-source-analysis-service
  datasource:
    url: jdbc:sqlite:./data/source-analysis.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    open-in-view: false
  threads:
    virtual:
      enabled: true

apixa:
  service-name: apixa-source-analysis-service
